@page
@model IndexModel
@{
    ViewData["Title"] = "Nastavení účtu";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.css" />
<link rel="stylesheet" href="~/css/Profile/profile.css" asp-append-version="true" />

<nav aria-label="breadcrumb" class="mt-4 mb-4">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/u/@Model.Username" class="text-decoration-none text-muted">Profil</a></li>
        <li class="breadcrumb-item active fw-bold text-dark">Nastavení účtu</li>
    </ol>
</nav>

<h2 class="fw-bold mb-4">@ViewData["Title"]</h2>

<div class="row g-4">
    <div class="col-lg-7">
        <div class="card shadow-sm border-0 rounded-4 h-100">
            <div class="card-body p-4 p-md-5">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h5 class="fw-bold mb-0">Osobní údaje</h5>
                    <button type="button" id="edit-btn" class="btn btn-primary rounded-pill px-4 shadow-sm" onclick="enableEdit()">
                        <i class="bi bi-pencil me-2"></i>Upravit
                    </button>
                </div>

                <form id="profile-form" method="post" enctype="multipart/form-data">
                    <input type="file" asp-for="Input.ProfilePicture" id="upload-photo" class="d-none" accept="image/*" onchange="previewImage(this)">

                    <div class="d-flex align-items-center mb-4 pb-3 border-bottom">
                        <div class="profile-pic-wrapper shadow-sm border me-3" onclick="document.getElementById('upload-photo').click()" title="Změnit profilovou fotku">
                            @if (!string.IsNullOrEmpty(Model.ProfilePictureUrl)) 
                            {
                                <img id="img-preview" src="@Model.ProfilePictureUrl" alt="Profilovka" class="profile-pic-img">
                            }
                            else
                            {
                                <div id="initials-placeholder" class="bg-primary text-white d-flex align-items-center justify-content-center w-100 h-100" style="font-size: 24px;">
                                    @(Model.Username?.Substring(0, 1).ToUpper() ?? "U")
                                </div>
                                <img id="img-preview" src="#" alt="Náhled" class="profile-pic-img d-none">
                            }
                            <div class="profile-pic-overlay"><i class="bi bi-camera-fill"></i></div>
                        </div>
                        <div>
                            <div class="fw-bold">Profilová fotografie</div>
                            <small class="text-muted">Klikněte na fotku pro změnu</small>
                        </div>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Username" class="form-control" disabled id="input-username"/>
                        <label asp-for="Input.Username">Uživatelské jméno</label>
                    </div>

                    <div class="form-floating mb-3">
                        <textarea asp-for="Input.Bio" class="form-control" style="height: 120px" disabled id="input-bio"></textarea>
                        <label asp-for="Input.Bio">O mně (Bio)</label>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control" disabled id="input-email"/>
                        <label asp-for="Input.Email">E-mailová adresa</label>
                    </div>

                    <div class="form-floating mb-4">
                        <input asp-for="Input.PhoneNumber" class="form-control" disabled id="input-phone"/>
                        <label asp-for="Input.PhoneNumber">Telefonní číslo</label>
                    </div>

                    <div id="save-actions" class="d-none">
                        <button id="save-btn" type="submit" class="btn btn-primary w-100 rounded-pill py-2 fw-bold shadow-sm">Uložit změny</button>
                        <button type="button" class="btn btn-link text-muted w-100 mt-2 text-decoration-none" onclick="location.reload()">Zrušit</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-lg-5">
        <div class="card shadow-sm border-0 rounded-4 mb-4">
            <div class="card-body p-4 p-md-5">
                <h5 class="fw-bold mb-4">Zabezpečení & Heslo</h5>
                <form id="change-password-form" method="post" asp-page-handler="ChangePassword">
                    <div class="form-floating mb-3">
                        <input asp-for="PasswordInput.OldPassword" class="form-control rounded-3" autocomplete="current-password" placeholder="Staré heslo" />
                        <label asp-for="PasswordInput.OldPassword">Současné heslo</label>
                        <span asp-validation-for="PasswordInput.OldPassword" class="text-danger small"></span>
                    </div>

                    <div class="form-floating mb-3">
                        <input asp-for="PasswordInput.NewPassword" class="form-control rounded-3" autocomplete="new-password" placeholder="Nové heslo" />
                        <label asp-for="PasswordInput.NewPassword">Nové heslo</label>
                        <span asp-validation-for="PasswordInput.NewPassword" class="text-danger small"></span>
                    </div>

                    <div class="form-floating mb-4">
                        <input asp-for="PasswordInput.ConfirmPassword" class="form-control rounded-3" autocomplete="new-password" placeholder="Potvrzení hesla" />
                        <label asp-for="PasswordInput.ConfirmPassword">Potvrzení nového hesla</label>
                        <span asp-validation-for="PasswordInput.ConfirmPassword" class="text-danger small"></span>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 rounded-pill py-2 fw-bold shadow-sm">
                        <i class="bi bi-key me-2"></i>Aktualizovat heslo
                    </button>
                </form>
            </div>
        </div>
        
        <div class="card shadow-sm border-0 rounded-4 border-start border-warning border-4">
            <div class="card-body p-4">
                <h6 class="fw-bold text-warning mb-3">Zablokovat účet</h6>
                <p class="small text-muted mb-4">Tato akce vás okamžitě odhlásí a zablokuje váš účet. Pro opětovné aktivování kontaktujte administrátora.</p>
                <button type="button" class="btn btn-outline-warning btn-sm rounded-pill w-100 py-2" data-bs-toggle="modal" data-bs-target="#lockAccountModal">
                    <i class="bi bi-lock me-2"></i>Zablokovat přístup
                </button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="lockAccountModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded-4 border-0 shadow-lg">
            <div class="modal-header border-0 py-4 px-4 pb-0">
                <h5 class="modal-title fw-bold"><i class="bi bi-exclamation-triangle-fill text-warning me-2"></i>Zablokovat účet?</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Zavřít"></button>
            </div>
            <div class="modal-body p-4">
                <p class="text-muted mb-0">Opravdu si přejete svůj účet zablokovat? Budete okamžitě odhlášeni a pro opětovný přístup budete muset kontaktovat podporu/administrátora.</p>
            </div>
            <div class="modal-footer border-0 p-4 pt-0">
                <button type="button" class="btn btn-light rounded-pill px-4" data-bs-dismiss="modal">Zrušit</button>
                <form method="post" asp-page-handler="LockAccount">
                    <button type="submit" class="btn btn-warning rounded-pill px-4 fw-bold">Ano, zablokovat</button>
                </form>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="errorModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-danger rounded-4">
            <div class="modal-header bg-danger text-white border-0 py-3 rounded-top-4">
                <h5 class="modal-title fw-bold"><i class="bi bi-exclamation-triangle-fill me-2"></i> Nastala chyba</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Zavřít"></button>
            </div>
            <div class="modal-body p-4">
                <div asp-validation-summary="All" class="text-danger fw-bold"></div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="cropModal" tabindex="-1" aria-hidden="true" data-bs-backdrop="static">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded-4 border-0 shadow-lg">
            <div class="modal-header border-0">
                <h5 class="modal-title fw-bold">Upravit fotku</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Zavřít"></button>
            </div>
            <div class="modal-body p-0">
                <div class="img-container" style="height: 400px; background-color: #f8f9fa;">
                    <img id="image-to-crop" src="" style="display: block; max-width: 100%;">
                </div>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-secondary rounded-pill px-4" data-bs-dismiss="modal">Zrušit</button>
                <button type="button" class="btn btn-primary rounded-pill px-4" id="crop-btn">Uložit</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.js"></script>
    <script src="~/js/Profile/profile.js" asp-append-version="true"></script>
}